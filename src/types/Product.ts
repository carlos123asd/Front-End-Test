export type Product = {
    id: string,
    brand: string,
    model: string,
    price: string,
    imgUrl: string,
}

export type ProductCart = {
    id: number,
    colorCode: number,
    storageCode: number,
}

export type ProductDetail = {
    id: string,
    brand: string,
    model: string,
    price: string,
    imgUrl: string,
}